#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
	        for (char n = 'a'; n <= 'z'; n++)
			putchar(n);
		for (char c = 'A' ; c <= 'Z' ; c++)
			putchar(c);
		putchar('\n');
		return (0);
}
